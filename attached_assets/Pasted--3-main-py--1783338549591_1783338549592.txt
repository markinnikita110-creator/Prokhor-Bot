Задача: исправить баг В3 в main.py — в блоке КОГОРТНЫХ напоминаний отправка психологу о когортной сессии не обёрнута в try/except, в отличие от уже исправленных индивидуальных напоминаний. Если психолог недоступен (заблокировал бота и т.п.) — падает весь блок обработки этой когортной сессии в текущем цикле reminder_loop, вместо того чтобы залогировать ошибку и продолжить с остальными.

Это маленькая, самодостаточная задача — НЕ начинай перенос когортных чек-инов/рассылок в core-слой, это отдельная будущая задача.

ЭТАП 0 — Аудит (код не менять)
1. Найди в main.py точный блок когортных напоминаний (не индивидуальных — те уже в core/services/reminders.py и уже с try/except). Покажи код целиком с точными номерами строк.
2. Укажи явно, какая именно строка/строки с bot.send_message(psych_id, ...) не обёрнуты в try/except.
3. Проверь, есть ли в этом же блоке отправка участникам когорты (не только психологу) — если она уже обёрнута в try/except по каждому участнику отдельно (как было в предыдущих отчётах про "try/except на участника уже есть, это ок") — подтверди это, чтобы я был уверен, что трогаем только психолога, не переделываем то, что уже работает.
4. Выведи результат текстом. ОСТАНОВИСЬ, дождись моего "продолжай".

ЭТАП 1 — Фикс (после подтверждения)
1. Оберни отправку психологу в try/except, по образцу того, как это уже сделано для индивидуальных напоминаний в core/services/reminders.py (тот же стиль: log.warning с указанием psych_id и session_id/cohort_id, само исключение не пробрасывается дальше).
2. Ничего больше в этом блоке не трогай — только эта одна отправка.

ЭТАП 2 — Отчёт
Построчное "было / стало" для этого конкретного места — как делали для всех предыдущих критичных фиксов в reminder_loop.

ЭТАП 3 — Чек-лист ручной проверки
- Сделать психолога недоступным для отправки (заблокировать бота от тестового аккаунта или подставить несуществующий chat_id), создать когортную сессию с напоминанием → напоминание психологу падает с ошибкой в логах (WARNING), но НЕ ломает обработку остальной когортной сессии — участники, которым напоминание должно было уйти, всё равно его получают
- Обычный сценарий (психолог доступен) → напоминание приходит как раньше, регрессии нет

Не переходи к переносу когортных чек-инов/рассылок или любым другим доменам в рамках этой задачи — только этот один фикс.