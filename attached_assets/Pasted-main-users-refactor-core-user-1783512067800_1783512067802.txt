main НЕ содержит перенос users — это ещё не смерджено. Актуальная работа 
в ветке refactor/core-users, она уже запушена на GitHub. Начни с этого:

git fetch origin
git checkout refactor/core-users
git pull origin refactor/core-users

Контекст: перенос домена "Пользователи/Профиль" в core-слой был начат 
предыдущей сессией и оборвался на середине (закончилась квота) во время 
массовой параллельной правки импортов сразу в 17+ файлах. Уже создано:
- core/db/users_repository.py
- core/services/users.py
Уже изменены (но нужно ПРОВЕРИТЬ, что каждое изменение доведено до конца, 
а не оборвано на середине): core/services/cohort_sessions.py, database.py, 
handlers/analytics.py, handlers/booking.py, handlers/booking_settings.py, 
handlers/checkins.py, handlers/client_sessions.py, handlers/clients.py, 
handlers/homework.py, handlers/legal.py, handlers/menu.py, handlers/notes.py, 
handlers/plans.py, handlers/sessions.py, handlers/settings.py, 
handlers/supervision.py, handlers/timezone.py.

ВАЖНО: main.py в это изменение НЕ попал (обрыв случился до того, как до 
него дошла очередь) — проверь, ссылается ли main.py на что-то из того, 
что было удалено/перенесено из database.py, и если да — доведи и его 
до соответствия.

Твоя задача:
1. Сначала — полная инвентаризация: пройдись по каждому из 17+ изменённых 
   файлов и по database.py, сравни с тем, что должно быть после переноса 
   (все функции работы с профилем психолога — язык, часовой пояс, 
   регистрация — должны идти через core.db.users_repository / 
   core.services.users, а не напрямую из database.py).
2. Найди все места, где импорт или вызов остался ссылаться на старый путь 
   (функцию, которая уже удалена или перемещена) — это конкретно то, что 
   могло сломаться из-за обрыва на середине.
3. Доведи ВСЕ найденные несостыковки до консистентного состояния — задача 
   считается завершённой, только когда во всём проекте нет ни одной ссылки 
   на старые, уже неактуальные пути импорта для функций профиля психолога.
4. Запусти бота (python3 main.py), убедись что стартует БЕЗ единой ошибки 
   импорта — это обязательное условие, не сдавай задачу, пока не добьёшься 
   чистого старта.
5. Дай отчёт: что было не докручено с прошлой сессии, что довёл до конца, 
   полный список финально изменённых файлов.
6. Чек-лист для меня: смена языка в настройках сохраняется и применяется 
   везде; смена часового пояса сохраняется и применяется в напоминаниях/
   тарифах; новый пользователь регистрируется нормально; /admin команды 
   работают как раньше.

Не начинай новых доменов (самозапись, FAQ, GDPR, админ-панель) — 
единственная цель сейчас: довести перенос users до полностью рабочего 
и консистентного состояния, закоммить и сообщить, что можно сливать в main.