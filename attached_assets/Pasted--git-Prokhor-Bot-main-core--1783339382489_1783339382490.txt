Контекст: работаем в git-репозитории Prokhor-Bot, main уже содержит полный core-рефакторинг (клиенты, заметки, домашка, чек-ины, обычные сессии, напоминания, базовая сущность когорт + вся безопасность owner-check, регулярные когортные сессии). Баг В3 (когортные напоминания без try/except на психолога) уже закрыт ранее — не проверяй и не трогай его заново.

Задача: перенести домен "Когортные чек-ины / рассылки" из handlers/cohorts.py в core-слой (core/db/cohort_checkins_repository.py + core/services/cohort_checkins.py), по той же архитектуре, что уже применена для остальных доменов. Работай строго поэтапно, останавливайся после каждого этапа и жди моего "продолжай". Веди работу в отдельной git-ветке (например, refactor/core-cohort-checkins), создавай её от актуального main. Не трогай main напрямую до полного завершения и проверки.

ВАЖНЫЙ КОНТЕКСТ БЕЗОПАСНОСТИ: в когортном домене несколько раз находили отсутствие owner-check (проверки, что cohort_id/session_id принадлежит вызывающему психологу) в разных обработчиках — каждый раз находились новые пропущенные места, даже после, казалось бы, финальной проверки. Чек-ины и рассылки — прямые кандидаты на ту же уязвимость (рассылка чужим участникам, чтение чужих ответов, настройка чек-инов в чужой когорте). Каждая перенесённая функция ОБЯЗАНА сохранить owner-check на своей текущей позиции (перед мутацией/чтением), используя verify_cohort_owner из core/db/cohorts_repository.py — не создавай новую версию проверки. В конце обязательно прогони собственный code-review именно на предмет owner-check, не полагайся только на визуальный перенос.

ЖЁСТКИЕ ПРАВИЛА:
- Только перенос существующего кода, без изменения поведения.
- Найденные по пути другие баги — не чинить тихо, вынести в отчёт и спросить меня.
- Не добавлять новых функций сверх описанного.
- После каждого этапа — список изменённых файлов и что стало короче в старых местах.
- Использовать core/utils/time.py, core/db/base.py, core/db/cohorts_repository.py, core/db/cohort_sessions_repository.py — не дублировать существующее.
- Проверить, нет ли в FSM state для рассылки/чек-инов паттерна set() (по аналогии с недавно найденным и исправленным багом с set() в FSM для регулярных сессий) — если найдёшь, исправь на list по той же схеме.
- Если задача окажется больше, чем можно закончить за один заход — сдай чистый частичный результат с явным указанием, где остановился, не начинай следующий крупный блок не закончив предыдущий.

ЭТАП 0 — Аудит (код не менять)
1. Найди в handlers/cohorts.py все функции, связанные с когортными чек-инами: настройка (интервал/расписание), отправка участникам (ручная и, если есть, автоматическая), сохранение ответа участника, чтение сводки/истории ответов.
2. Найди все функции, связанные с рассылкой сообщений участникам когорты (broadcast), если это отдельный механизм от чек-инов.
3. Для КАЖДОЙ функции — построчная карта: есть ли owner-check, на какой позиции относительно мутации/чтения. Учти, что cv2_ci_got_interval и cv2_broadcast_send уже получали owner-check в одном из предыдущих раундов фиксов — подтверди, что это так и не потерялось.
4. Проверь FSM state на предмет set() (см. правило выше).
5. Проверь наличие логики эскалации/алертов по ключевым словам в ответах на когортный чек-ин.
6. Проверь прямые SQL-запросы к таблицам когортных чек-инов внутри хендлеров, минуя уже существующие репозитории.
7. Выведи весь результат текстом, включая полную карту owner-check'ов. ОСТАНОВИСЬ, дождись моего "продолжай".

ЭТАП 1 — Перенос
1. core/db/cohort_checkins_repository.py — CRUD для когортных чек-инов и рассылок, используя core/db/base.py. Owner-check переносится на ту же позицию без изменений.
2. core/services/cohort_checkins.py — если есть многошаговые сценарии, объединяющие несколько операций — вынеси сюда как сервисные функции.
3. Удали перенесённые функции из handlers/cohorts.py, поправь импорты и вызовы.

ЭТАП 2 — Отчёт с построчной проверкой owner-check'ов
Построчное "было/стало" для каждого owner-check из карты Этапа 0 — не общий diff.

ЭТАП 3 — Собственный code-review на предмет пропущенных owner-check
Прогони отдельный проход именно по этому вопросу (как делали в прошлый раз при интеграции, где нашлись ещё 4 пропущенных места) — явно подтверди результат, сколько всего owner-check найдено и что ни один не пропущен.

ЭТАП 4 — Чек-лист ручной проверки
Функциональность:
- Настроить когортный чек-ин (интервал/расписание) → сохраняется
- Отправить чек-ин участникам вручную → все получили
- Участник отвечает → ответ сохранился, виден в сводке психологу
- Рассылка сообщения участникам когорты → все получили

Безопасность (обязательно):
- С аккаунта Б (посторонний) — попытка настроить чек-ин в чужой когорте А → отказ, SECURITY warning в логах
- С аккаунта Б — попытка отправить рассылку в чужую когорту А → отказ, SECURITY warning
- С аккаунта Б — попытка прочитать сводку ответов чужой когорты А → отказ

Не переходи к переносу супервизии, тарифов, самозаписи или других доменов в рамках этой задачи.