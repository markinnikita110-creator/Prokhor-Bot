Задача: перенести домен "Заметки / SOAP-записи" в core-слой, по той же схеме, что уже сделали для клиентов. Работай строго поэтапно, останавливайся после каждого этапа и жди моего "продолжай". Веди работу в отдельной git-ветке (например, refactor/core-notes), не трогай main.

ЖЁСТКИЕ ПРАВИЛА (те же, что в прошлый раз):
- Никакого изменения поведения — только перенос существующего кода.
- Если найдёшь баг по пути — не чини тихо, вынеси в отдельный пункт отчёта и спроси меня.
- Не добавляй новых функций и не улучшай логику "по пути".
- После каждого этапа показывай список изменённых файлов и что стало короче в старых местах.
- core/utils/time.py и core/db/base.py уже созданы на прошлом шаге (перенос клиентов) — используй их, не создавай заново и не дублируй.

ЭТАП 0 — Аудит (код не менять)
1. Найди в database.py все функции, связанные с заметками и SOAP-записями (создание, чтение одной/списка по клиенту, редактирование, удаление, если такое есть). Учти, что в проекте может быть разделение на "обычная заметка" и "структурированная SOAP-заметка" — если это две разные сущности/таблицы, зафиксируй обе отдельно, не смешивай в одну.
2. Найди во всех handlers/*.py, где эти функции вызываются — список (файл, функция).
3. Проверь, есть ли в текущей логике бизнес-правила при создании/редактировании заметки (например, лимит по тарифу на количество заметок, ограничение длины текста, автоматическая привязка к последней сессии клиента) — перечисли их явно, это кандидаты на core/services/notes.py, а не просто CRUD.
4. Проверь, ссылаются ли заметки на sessions (session_id) или существуют независимо от привязки к конкретной сессии — это важно для следующего шага (перенос сессий), явно укажи связь.
5. Проверь, есть ли прямые SQL-запросы к таблице заметок внутри хендлеров, минуя database.py — отметь отдельно, если есть.
6. Выведи результат текстом. ОСТАНОВИСЬ, дождись "продолжай".

ЭТАП 1 — Перенос
1. core/db/notes_repository.py — перенеси туда все функции работы с таблицей(ами) заметок/SOAP из database.py, используя core/db/base.py для подключения. Без изменения логики.
2. core/services/notes.py — вынеси сюда найденные на Этапе 0 бизнес-правила (лимиты по тарифу и т.п.), которые внутри вызывают core/db/notes_repository.py. Если правил нет — тонкий файл-обёртка, не выдумывай логику.
3. Удали перенесённые функции из database.py (без дублирующихся копий).
4. Поправь импорты во всех handlers/*.py, где заметки создавались/читались — на core/services/notes.py.

ЭТАП 2 — Отчёт и чек-лист ручной проверки
Выведи список изменённых файлов и diff-сводку, как в прошлый раз, плюс чек-лист для меня:
- Создать обычную заметку клиенту → появилась в его карточке/истории
- Создать SOAP-заметку (если это отдельный сценарий) → сохранилась со всеми полями (Subjective/Objective/Assessment/Plan)
- Отредактировать существующую заметку → изменения сохранились
- Удалить заметку (если такая функция есть) → пропала
- Если заметка привязана к сессии — открыть карточку той сессии и проверить, что заметка там видна
- Если есть лимит по тарифу на заметки (Start vs Pro) — проверить, что лимит по-прежнему срабатывает как раньше
- Полное удаление практики психолога (ранее чинили этот баг) → заметки клиента реально удаляются вместе с остальным, не остаются "осиротевшими"

Не переходи к переносу других доменов в рамках этой задачи.