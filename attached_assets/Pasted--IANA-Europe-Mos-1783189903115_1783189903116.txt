Задача: часовой пояс психолога хранится непоследовательно — где-то валидным IANA-именем ('Europe/Moscow'), где-то как строка смещения ('UTC+3', 'UTC'), которая не работает с ZoneInfo и вызывает исключение. Из-за этого конвертация времени тихо откатывается на голое UTC там, где перехватывается ошибка. Также обнаружено расхождение: у одного и того же психолога (psych_id=849084673) в таблице psychologists хранится 'Europe/Moscow', а в таблице booking_profile для него же — 'UTC+3' (разные значения одновременно). Работай поэтапно, останавливайся после каждого этапа и жди подтверждения.

ЭТАП 0 — Аудит (без изменений кода)
1. Найди все места в коде (экраны настроек, где психолог выбирает часовой пояс, и booking-профиль), где ПИШЕТСЯ значение в поле timezone — покажи, откуда берутся строки вида "UTC+3", "UTC" (это явно не то, что возвращает нормальный список IANA-зон — похоже на ручной список выбора вроде "UTC+1, UTC+2, UTC+3..." вместо выбора настоящих зон).
2. Найди все места, где ЧИТАЕТСЯ поле timezone и передаётся в ZoneInfo(...) — проверь, обёрнуто ли это в try/except, и что происходит при ошибке (тихий фолбэк на UTC, или явная ошибка).
3. Явно ответь: почему у одного психолога (849084673) значения в psychologists и booking_profile разные — это два независимых поля, которые заполняются в разных местах интерфейса, или должно быть одно значение, продублированное в двух таблицах по ошибке?
4. Выведи результат текстом, дождись моего "продолжай".

ЭТАП 1 — Нормализация источника выбора часового пояса
1. Замени экран(ы) выбора часового пояса (везде, где сейчас предлагается список вида "UTC+1/UTC+2/UTC+3...") на выбор из настоящих IANA-имён — минимум для русскоязычной аудитории: "Europe/Kaliningrad" (UTC+2), "Europe/Moscow" (UTC+3), "Europe/Samara" (UTC+4), "Asia/Yekaterinburg" (UTC+5), "Asia/Omsk" (UTC+6), "Asia/Krasnoyarsk" (UTC+7), "Asia/Irkutsk" (UTC+8), "Asia/Yakutsk" (UTC+9), "Asia/Vladivostok" (UTC+10) — подписи кнопок можно оставить в привычном виде "МСК (UTC+3)", но в БД сохранять именно IANA-имя ("Europe/Moscow"), не строку смещения.
2. Добавь валидацию перед сохранением: любое новое значение timezone должно проверяться через попытку ZoneInfo(value) — если бросает исключение, не сохранять и не давать этому попасть в БД.
3. Если у психолога должно быть ОДНО значение часового пояса, а не два независимых (в psychologists и в booking_profile) — убери дублирование: booking_profile должен брать значение из psychologists как единственный источник правды, не хранить свою копию (или наоборот, в зависимости от того, что выяснится на Этапе 0).

ЭТАП 2 — Миграция уже существующих некорректных значений
1. Найди все записи, где timezone НЕ является валидным IANA-именем (то есть ZoneInfo(value) падает с ошибкой) — это как минимум все значения вида 'UTC+N' и голое 'UTC'.
2. Для значений вида 'UTC+N' — сопоставь с наиболее вероятным IANA-именем из списка в Этапе 1 (UTC+3 → Europe/Moscow и т.д.) и обнови эти записи напрямую в БД.
3. Для значений, где буквально 'UTC' (то есть по факту "не задано", а не осознанный выбор часового пояса UTC+0) — НЕ проставляй Europe/Moscow молча. Вместо этого при следующем заходе такого пользователя в бота — показать ему одноразовое сообщение "Пожалуйста, подтвердите ваш часовой пояс" с тем же экраном выбора из Этапа 1, чтобы он выбрал сам, а не получил угаданное значение.
4. Обязательно сделай резервную копию БД перед этой миграцией (простой cp prokhor.db prokhor.db.backup_before_tz_fix) и явно скажи мне, что бэкап сделан, прежде чем запускать UPDATE-запросы.

ЭТАП 3 — Убрать тихий фолбэк на UTC при ошибке конвертации
Замени любой try/except вокруг ZoneInfo(...), который сейчас молча возвращает UTC-время при ошибке, на явное поведение: если конвертация не удалась — залогировать ошибку с указанием psych_id/client_id и некорректного значения timezone, и в сообщении пользователю явно показать "не удалось определить ваш часовой пояс, отображено время UTC — пожалуйста, проверьте настройки часового пояса", а не тихо показывать неверное время как будто оно правильное.

ЭТАП 4 — Проверка
После миграции — повтори тест: удали у тестового психолога (dip_garant, у которого было 'UTC+3') тариф, выдай на 1 день, проверь что показанное время теперь совпадает с реальным московским временем без расхождения в 3 часа. Также проверь psych_id=849084673 — что теперь psychologists и booking_profile показывают одинаковый, валидный часовой пояс.

Обязательно сделай бэкап БД перед Этапом 2 и явно подтверди это мне до выполнения UPDATE-запросов на реальных данных пользователей.