Задача: перенести домен "Когорты — базовая сущность" (создание когорты, участники, посещаемость) из database.py и handlers/ в core-слой (core/db/cohorts_repository.py + core/services/cohorts.py), по той же архитектуре, что уже применена для клиентов, заметок, домашних заданий, чек-инов, сессий и напоминаний. Работай в отдельной git-ветке refactor/core-cohorts-base, не трогай main.

Сначала проверь текущее состояние ветки — если предыдущая сессия уже что-то начала (аудит, частично созданный репозиторий, частично переведённые handlers), продолжи с этого места, не начинай заново.

Область переноса — строго: создание/чтение/редактирование/архивация когорты, добавление и удаление участников, чтение списка участников, посещаемость (если она относится к самой когорте, а не к когортным сессиям). НЕ переносить в этой задаче: регулярные когортные сессии, когортные чек-ины/рассылки, супервизию — это отдельные будущие домены.

Найди все функции работы с когортами в database.py, все места их вызова в handlers/*.py, все прямые SQL-запросы к таблицам когорт внутри хендлеров (если такие есть, минуя database.py), и бизнес-правила лимитов по тарифу на количество когорт/участников (Start vs Pro) — перенеси правила в core/services/cohorts.py, а сам CRUD — в core/db/cohorts_repository.py, используя уже существующий core/db/base.py для подключения. Удали перенесённые функции из database.py, поправь импорты в handlers/*.py.

Отдельно зафиксируй в отчёте (но НЕ чини сейчас) баг В3 — в блоке когортных напоминаний в main.py отправка психологу не обёрнута в try/except, в отличие от индивидуальных напоминаний. Просто подтверди точным указанием файла/строки, что баг на месте — фикс будет отдельной задачей при переносе когортных чек-инов/рассылок.

Никакого изменения поведения — только перенос существующего кода как есть. Если найдёшь баг помимо В3 — не чини тихо, вынеси в отдельный пункт отчёта.

В конце дай отчёт: список изменённых/созданных файлов, что стало короче в database.py и handlers/, и чек-лист для моей ручной проверки:
- Создать когорту → появилась в списке
- Добавить участника → появился в списке
- Удалить участника → пропал
- Список когорт психолога → отображается верно
- Лимит по тарифу на когорты/участников (Start) → срабатывает как раньше
- Архивировать/удалить когорту (если реализовано) → работает как раньше
- Полное удаление практики психолога → когорты и участники удаляются вместе с остальным

Если задача окажется больше, чем можно закончить целиком за один заход — лучше сдай чистый частичный результат (например, только репозиторий готов, handlers ещё нет) с явным указанием, где остановился, чем начинай следующий крупный блок не закончив предыдущий.