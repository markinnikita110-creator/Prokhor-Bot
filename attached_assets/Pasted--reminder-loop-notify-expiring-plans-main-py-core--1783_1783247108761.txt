Задача: перенести домен "Напоминания" (reminder_loop и notify_expiring_plans из main.py) в core-слой. Это самый чувствительный перенос из всех до сих пор — за этот проект уже несколько раз чинили именно этот код (дублирование напоминаний, смешение UTC/локального времени, захардкоженный текст, тихий фолбэк на UTC из-за невалидного часового пояса). ГЛАВНАЯ ЦЕЛЬ этой задачи — перенести код БЕЗ ОТКАТА хотя бы одного из уже сделанных фиксов. Работай строго поэтапно, останавливайся после каждого этапа и жди моего "продолжай". Веди работу в отдельной git-ветке (например, refactor/core-reminders), не трогай main.

ЖЁСТКИЕ ПРАВИЛА (не меняются):
- Только перенос существующего кода, без изменения поведения.
- Найденные по пути баги — не чинить тихо, вынести в отчёт и спросить меня.
- Не добавлять новых функций.
- После каждого этапа — список изменённых файлов и что стало короче в старых местах.
- Использовать core/utils/time.py, core/db/base.py, core/services/sessions.py, core/services/checkins.py — все уже перенесённые ранее домены, не дублировать и не создавать вторую версию их функций.

ЭТАП 0 — Аудит (код не менять, это критичный аудит, не торопись)
1. Найди в main.py весь код reminder_loop() и notify_expiring_plans() — приведи их текущий вид целиком, построчно.
2. Явно зафиксируй список фиксов, которые должны остаться нетронутыми после переноса (сверься с реальным текущим кодом, не с историей разговора):
   - Использование db.now_utc() / core/utils/time.py для всех сравнений времени (не datetime.now() без явного UTC)
   - try/except индивидуально вокруг каждой отправки bot.send_message (психологу и клиенту отдельно) — одна неудачная отправка не должна прерывать обработку остальных сессий в этом же цикле
   - Флаги reminded_24h/reminded_1h выставляются после обеих попыток отправки (или раздельно, если это было сделано раздельно) — без дублирования напоминаний
   - Текст уведомления об истечении тарифа берётся через t(lang, "plan_expiring_tomorrow", ...), не хардкод
   - Конвертация времени сессии в часовой пояс конкретного получателя (психолога/клиента) при формировании текста напоминания — через core/utils/time.py, с учётом недавнего фикса про валидные IANA-имена
3. Проверь, какие функции reminder_loop вызывает для получения данных о сессиях — идут ли они уже через core/services/sessions.py (после недавнего переноса), или где-то остался прямой SQL/старый импорт из database.py, который не обновился.
4. Проверь, использует ли reminder_loop что-либо из чек-инов (в аудите чек-инов ранее было сказано, что автозапуска по расписанию нет — подтверди ещё раз, что reminder_loop их не трогает вообще).
5. Проверь, как reminder_loop и notify_expiring_plans запускаются — через asyncio.create_task при старте бота, через APScheduler, или как-то иначе — это важно перенести аккуратно, не сломав сам механизм периодического запуска.
6. Проверь пересечение с когортными напоминаниями (если они в отдельном блоке кода) — явно разграничь: в этой задаче переносим только индивидуальные напоминания (sessions) и notify_expiring_plans (тарифы); когортные напоминания — отдельный будущий домен, не трогать сейчас, если они физически отдельный кусок кода.
7. Выведи ВЕСЬ результат текстом. ОСТАНОВИСЬ, дождись моего "продолжай" — не переходи к переносу без явного подтверждения, мне нужно самому свериться со списком фиксов из пункта 2 построчно.

ЭТАП 1 — Перенос
1. core/services/reminders.py — перенеси сюда reminder_loop() и notify_expiring_plans() как функции (без флага "loop" в названии — сама функция один проход, отдельно решим на Этапе 2, как её периодически вызывать).
2. Все обращения к данным сессий/тарифов внутри перенесённого кода — переведи на core/services/sessions.py и на существующие функции тарифов в database.py (тарифы ещё не переносили в core — это нормально, оставь как есть, если тарифный домен ещё не тронут, просто не создавай новых прямых SQL-запросов вместо существующих вызовов).
3. Каждый try/except вокруг отправки — перенеси КАК ЕСТЬ, построчно сверяя со списком из Этапа 0, пункт 2. Если сомневаешься, что перенос точно сохранил структуру try/except — покажи мне старый и новый код рядом (до/после) именно этого участка, прежде чем продолжать.
4. В main.py — замени определение reminder_loop()/notify_expiring_plans() на импорт из core/services/reminders.py, сохрани механизм периодического запуска (asyncio.create_task/цикл with sleep) в main.py — сама "обёртка с бесконечным while + sleep" может остаться в main.py, если так проще, но тело одного прохода должно быть в core/.

ЭТАП 2 — Отчёт с построчным сравнением критичных фиксов
Не общий diff, а ЯВНО построчно покажи:
1. Было / стало для try/except вокруг отправки психологу.
2. Было / стало для try/except вокруг отправки клиенту.
3. Было / стало для места, где выставляются reminded_24h/reminded_1h.
4. Было / стало для datetime.now()/db.now_utc() в notify_expiring_plans.
5. Было / стало для текста уведомления об истечении тарифа (t(lang, ...) вместо хардкода).
6. Подтверди явно: "все 5 пунктов выше идентичны по поведению до и после переноса" — или явно укажи, если хоть один изменился, и почему.

ЭТАП 3 — Чек-лист ручной проверки (самый важный из всех до сих пор, не сокращать)
- Создать тестовую сессию с датой через 24+ часа → дождаться/симулировать момент напоминания за 24ч → психолог и клиент оба получили напоминание, время в тексте показано в ИХ ЛИЧНОМ часовом поясе, не в UTC и не в серверном
- То же самое для напоминания за 1ч
- Заблокировать бота от имени тестового клиента (или использовать несуществующий/недоступный chat_id) → напоминание психологу всё равно приходит, ошибка отправки клиенту не блокирует остальные сессии в этом цикле
- Проверить, что после однажды отправленного напоминания флаг выставлен и повторной отправки на следующей итерации цикла НЕ происходит (дубль не воспроизводится)
- Выдать тестовому психологу тариф на 1 день, дождаться/симулировать срабатывание notify_expiring_plans → время в уведомлении соответствует его личному часовому поясу, текст на его языке (проверить и RU, и EN, если есть тестовый аккаунт с EN)
- Перезапустить бота (raw restart, как обычно бывает на телефоне) → цикл напоминаний запускается снова корректно, не падает на старте из-за изменённых импортов

Не переходи к переносу когорт или тарифов в рамках этой задачи. Если на Этапе 0 или Этапе 1 возникнет любая неопределённость насчёт того, сохраняется ли конкретный фикс — остановись и спроси меня, не угадывай.