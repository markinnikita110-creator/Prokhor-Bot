Ты опытный Python-разработчик. У меня есть Telegram-бот "Прохор" 
на aiogram 3.x с SQLite (sqlite3). Мне нужно добавить юридическую 
защиту перед запуском в тест с реальными пользователями.

ЗАДАЧА — внеси следующие изменения в существующий код:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. СОГЛАСИЕ ПРИ /start
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Перед любой логикой /start проверяй: дал ли пользователь согласие
- Если нет — показывай текст согласия с двумя кнопками: 
  "✅ Принимаю условия" и "❌ Не принимаю"
- Если нажал "Не принимаю" — бот отвечает что не может работать 
  без согласия и останавливается
- Если нажал "Принимаю" — сохраняй в SQLite:
  user_id (integer), accepted_at (text, ISO формат), version (text, "1.0")
  и продолжай обычный онбординг
- Создай таблицу user_consents если не существует:
  CREATE TABLE IF NOT EXISTS user_consents (
    user_id INTEGER PRIMARY KEY,
    accepted_at TEXT NOT NULL,
    version TEXT NOT NULL DEFAULT '1.0'
  )
- При каждом /start проверяй наличие записи в user_consents — 
  если уже есть, согласие не показывай повторно

Текст согласия для показа пользователю:
"""
👋 Добро пожаловать в Прохора!

Прохор — инструмент для организации административной рутины 
специалистов (психологов, коучей, педагогов).

⚠️ Прохор не является медицинской информационной системой 
и не хранит медицинскую документацию.

🔒 Для работы сохраняется ваш Telegram ID и данные, 
которые вы вносите самостоятельно.

📄 Используя Прохора для работы с клиентами, вы подтверждаете, 
что самостоятельно получили их согласие на обработку данных.

Подробнее: /privacy
"""

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. КОМАНДА /privacy
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Добавь хендлер команды /privacy. Сначала бот отправляет 
текстовое сообщение:

"""
🔒 Политика конфиденциальности

Прохор сохраняет:
- Ваш Telegram ID
- Данные, которые вы вносите сами: имена клиентов, 
  рабочие заметки, расписание

Прохор НЕ сохраняет:
- Содержимое ваших переписок в Telegram
- Медицинские диагнозы или заключения
- Аудио и видео материалы

Ваши права:
- Удалить все свои данные: /delete_my_data

Прохор не является медицинской информационной системой.
Специалист самостоятельно несёт ответственность за данные 
своих клиентов.

Версия 1.0
"""

Затем сразу отдельным сообщением отправляй файл 
(document) с названием privacy_policy.pdf — путь к файлу: 
./documents/privacy_policy.pdf
Если файл не найден — пропускай отправку файла без ошибки, 
только логируй warning.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. КОМАНДА /delete_my_data
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Добавь хендлер /delete_my_data:
- Показывай предупреждение с двумя кнопками: 
  "🗑 Да, удалить всё" и "Отмена"
- При подтверждении: удаляй ВСЕ строки где user_id = 
  telegram_id пользователя из ВСЕХ таблиц базы данных
- Для этого автоматически найди все таблицы в БД через:
  SELECT name FROM sqlite_master WHERE type='table'
  и для каждой таблицы попробуй выполнить 
  DELETE FROM {table} WHERE user_id = ?
  Если в таблице нет колонки user_id — просто пропускай её
- После удаления отвечай: "✅ Все ваши данные удалены. 
  Если захотите вернуться — напишите /start"
- При отмене: "Отменено. Ваши данные на месте."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. КОМАНДА /terms (по аналогии с /privacy)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Добавь хендлер /terms — отправляет файл ./documents/terms.pdf
Если файл не найден — отвечай: 
"📄 Условия использования будут доступны в ближайшее время."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Стек: Python, aiogram 3.x, sqlite3
- Все callback_data для новых кнопок с уникальным префиксом 
  "legal_" чтобы не конфликтовать с существующими
- Не ломай существующую логику бота — только добавляй
- Все обращения к БД оборачивай в try/except с logging
- Создай папку ./documents/ если не существует
- Добавь все новые команды в список команд бота 
  (set_my_commands) если он уже есть в коде

Покажи только изменённые/добавленные части кода с 
комментариями где именно их вставить в существующий файл.