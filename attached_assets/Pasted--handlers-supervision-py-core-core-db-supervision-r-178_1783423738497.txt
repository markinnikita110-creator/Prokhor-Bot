Задача: перенести домен "Супервизия" из handlers/supervision.py в core-слой 
(core/db/supervision_repository.py + core/services/supervision.py при 
необходимости), плюс добавить предупреждение про анонимизацию при создании 
кейса. Работай поэтапно, останавливайся после каждого этапа, жди "продолжай". 
Отдельная ветка (refactor/core-supervision), от актуального main.

Контекст из аудита: домен простой — личный дневник психолога (кейсы привязаны 
только к psychologist_id, никакой связи с когортами/участниками нет). 
Owner-check уже везде на месте и на правильных позициях (sup_close_case перед 
UPDATE, /supervision_logbook и /supervision_progress через WHERE 
psychologist_id = ?). Это НЕ трогать и НЕ переделывать — только перенести 
как есть, сохранив owner-check на тех же позициях.

ЭТАП 1 — Перенос
core/db/supervision_repository.py — перенеси 4 SQL-операции (INSERT нового 
кейса, SELECT logbook, SELECT progress с status='open', SELECT+UPDATE close) 
из handlers/supervision.py, используя core/db/base.py. Owner-check остаётся 
на прежней позиции относительно мутации. Удали inline SQL из 
handlers/supervision.py, поправь импорты.

ЭТАП 2 — Предупреждение про анонимизацию
В sup_case_start (первый шаг FSM, /supervision_case) — добавь перед запросом 
client_alias текст-предупреждение через translations.py (RU и EN), например 
по смыслу: "Не указывайте настоящее имя клиента — используйте псевдоним или 
инициалы, кейс сохраняется в вашем личном журнале". Только текст, без 
валидации содержимого (не пытайся угадывать/фильтровать реальные имена — 
это ненадёжно и не входит в задачу, просто предупреждение перед вводом).

ЭТАП 3 — Отчёт
Список изменённых файлов, что стало короче, новый текст предупреждения 
(RU и EN) для проверки.

ЭТАП 4 — Чек-лист ручной проверки
- Создать кейс (/supervision_case) → предупреждение показывается перед 
  вводом псевдонима, кейс сохраняется как раньше
- /supervision_logbook → показывает только свои кейсы
- /supervision_progress → только открытые свои кейсы
- Закрыть кейс (sv_close_) → работает как раньше
- Чужой аккаунт не может закрыть чужой кейс (owner-check не потерялся при 
  переносе) — проверить явно

Не переходи к другим доменам (тарифы, пользователи, самозапись, FAQ, GDPR, 
админ-панель) в рамках этой задачи.