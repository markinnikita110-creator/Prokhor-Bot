Задача: перенести домен "Регулярные когортные сессии" из handlers/cohorts.py в core-слой (core/db/cohort_sessions_repository.py + core/services/cohort_sessions.py), по той же архитектуре, что уже применена для клиентов, заметок, домашних заданий, чек-инов, сессий, напоминаний и базовой сущности когорт. Работай в отдельной git-ветке (например, refactor/core-cohort-sessions), не трогай main.

ВАЖНЫЙ КОНТЕКСТ: в предыдущей сессии этого же проекта была закрыта критическая уязвимость безопасности во всём когортном домене — во все обработчики и приватные функции, включая _create_cohort_session, _get_cohort_sessions, _update_session_field, _finalize_schedule, _finalize_recurring_schedule, добавлены owner-check'и (проверка, что cohort_id/session_id принадлежит вызывающему психологу) с логированием попыток доступа (SECURITY: warnings). Эти проверки — КРИТИЧНЫ и должны быть перенесены В ТОЧНОСТИ как есть вместе с функциями, без единого пропуска. Если при переносе какой-либо owner-check потеряется или окажется не на своём месте (например, после проверки вместо перед мутацией, как было с catt_mark в прошлый раз) — это регрессия безопасности, а не просто баг. Сверяй каждую перенесённую функцию на наличие owner-check построчно, не полагаясь на то, что "перенос кода автоматически перенесёт и проверку".

ЖЁСТКИЕ ПРАВИЛА (не меняются):
- Только перенос существующего кода, без изменения поведения (owner-check'и переносятся КАК ЕСТЬ, не переписываются).
- Найденные по пути баги — не чинить тихо, вынести в отчёт и спросить меня.
- Не добавлять новых функций.
- После каждого этапа — список изменённых файлов и что стало короче в старых местах.
- Использовать core/utils/time.py, core/db/base.py, core/db/cohorts_repository.py (verify_cohort_owner отсюда) — не дублировать, не создавать вторую версию проверки владельца.
- Если задача окажется больше, чем можно закончить целиком за один заход — сдай чистый частичный результат с явным указанием, где остановился, а не начинай следующий крупный блок не закончив предыдущий.

ЭТАП 0 — Аудит (код не менять)
1. Найди в handlers/cohorts.py ВСЕ функции, относящиеся к когортным сессиям: _create_cohort_session, _get_cohort_sessions, _get_scheduled_sessions, _get_upcoming_sessions, _get_session, _update_session_field, _delete_cohort_session, _get_cohort_for_session, _delete_recurring_rule, generate_recurring_cohort_sessions, _finalize_schedule, _finalize_recurring_schedule, и все обработчики вида csdt_/cstp_/cslk_/csdl_/csdy_/cspz_/csrl_/csry_ (те самые, где недавно стандартизировали логирование).
2. Для КАЖДОЙ из этих функций явно укажи: есть ли там owner-check (после недавнего фикса безопасности), и на какой именно строке относительно остальной логики функции (до или после мутации/чтения данных) — это твоя карта того, что нужно сохранить в точности при переносе.
3. Проверь связь с посещаемостью (attendance) — она уже была частично перенесена в core/db/cohorts_repository.py при переносе базовой сущности когорт (upsert_attendance, get_attendance_for_session, seed_attendance_for_session) — проверь, не пересекается ли что-то из сессионного кода с этими уже перенесёнными функциями, и если да — используй их, не создавай дублирующиеся версии.
4. Проверь связь с напоминаниями — есть ли в reminder_loop/core/services/reminders.py обращения к когортным сессиям (для отправки напоминаний об их предстоящих датах) — если да, отметь явно, но не переноси эту часть, только зафиксируй как точку интеграции.
5. Проверь прямые SQL-запросы к таблице(ам) когортных сессий внутри хендлеров помимо перечисленных private-функций.
6. Выведи результат текстом со всей картой owner-check'ов из пункта 2. ОСТАНОВИСЬ, дождись моего "продолжай" — этот аудит критичен для того, чтобы не потерять фиксы безопасности при переносе.

ЭТАП 1 — Перенос
1. core/db/cohort_sessions_repository.py — перенеси все функции CRUD для когортных сессий (create, get списки, get одна, update поля, delete, delete recurring rule, generate recurring occurrences), используя core/db/base.py. КАЖДЫЙ owner-check переноси на ТОЧНО ТУ ЖЕ позицию относительно остальной логики, что была в оригинале — не переставляй "для красоты структуры".
2. core/services/cohort_sessions.py — вынеси сюда _finalize_schedule и _finalize_recurring_schedule как сервисные функции (это уже не чистый CRUD, а сценарии, объединяющие несколько шагов) — используют core/db/cohort_sessions_repository.py. Owner-check и здесь переносится без изменений позиции.
3. Удали перенесённые функции из handlers/cohorts.py.
4. Поправь импорты и вызовы в handlers/cohorts.py и любых других handlers/*.py, где эти функции вызывались.

ЭТАП 2 — Отчёт с явной проверкой сохранности owner-check'ов
Не просто общий diff — построчно подтверди для каждой перенесённой функции из списка Этапа 0, пункт 2: "owner-check на месте, позиция не изменилась" — или явно укажи, если что-то изменилось и почему.

ЭТАП 3 — Чек-лист ручной проверки
Функциональность (как раньше):
- Создать регулярную когортную сессию (выбор дней недели) → следующие occurrences генерируются как раньше
- Пауза/возобновление регулярной когортной сессии → работает как раньше
- Редактирование поля сессии (дата/время/тема) → сохраняется как раньше
- Удаление сессии и удаление правила повторения → работает как раньше

Безопасность (повторно, после переноса — не доверяй, что перенос сохранил всё автоматически):
- С аккаунта Б (посторонний) попытаться отредактировать/удалить сессию когорты аккаунта А через прямой callback с session_id А → отказ, SECURITY warning в логах, как было до переноса
- С аккаунта Б попытаться создать сессию в чужой когорте А (если это технически достижимо через callback без владения) → отказ

Не переходи к переносу когортных чек-инов/рассылок или супервизии в рамках этой задачи.